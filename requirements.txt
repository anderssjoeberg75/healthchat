# HealthChat — dependencies for the shared core modules in this directory
# (ai_client, garmin_db, garmin_handler, fitbit/strava/withings handlers,
# calorie_calc). The web server adds its own on top: see webapp/requirements.txt.

# Health data sources
garth==0.4.46              # Garmin Connect API wrapper
garminconnect>=0.2.0
requests==2.32.3           # HTTP for Fitbit / Strava / Withings / Ollama

# AI providers
openai==1.58.1             # OpenAI, xAI, Azure OpenAI and Ollama (OpenAI-compatible)
anthropic==0.39.0          # Anthropic Claude API
google-genai==0.3.0        # Google Gemini (new SDK)

# Report export
python-docx==1.1.2         # Word document creation
reportlab==4.2.5           # PDF generation
Pillow==11.0.0             # Image processing
